# Twelve Data (server-only). Copy to `.env` for local `bun run dev` / `npm run dev`.
# Never commit `.env`. For Wrangler: use `.dev.vars` (local) or `wrangler secret put TWELVEDATA_API_KEY`.
TWELVEDATA_API_KEY=
